spring:
  jpa:
    properties:
      hibernate:
        format_sql: false

  h2:
    console:
      enabled: false

logging:
  level:
    root: WARN
    com.switchwon.payment: INFO
    org.hibernate.SQL: OFF
